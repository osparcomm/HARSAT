# Identifies functions needed for parallel processing in `run_assessment`

Identifies functions needed for parallel processing in `run_assessment`

## Usage

``` r
parallel_objects(imposex = FALSE)
```

## Arguments

- imposex:

  A logical which, if TRUE, picks out extra functions required for an
  imposex assessment; defaults to FALSE

## Value

A vector of character strings of function names
