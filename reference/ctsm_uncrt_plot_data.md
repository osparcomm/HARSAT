# Generates a relative uncertainty plot

Generates a relative uncertainty plot

## Usage

``` r
ctsm_uncrt_plot_data(uncrt_obj, det_id)
```

## Arguments

- uncrt_obj:

  an uncertainty object

- det_id:

  a list of determinand ids
